# Pick whichever providers you want; the sidecar advertises what it can import.

# Claude through the Claude Code harness (uses your Claude login, no API key).
claude-agent-sdk>=0.1.0

# Claude through the Anthropic API (needs ANTHROPIC_API_KEY).
anthropic>=0.40.0

# Everything else: OpenAI, Gemini, Ollama, Bedrock, ...
litellm>=1.50.0
